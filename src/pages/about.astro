---
import '../styles/global.css';
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "Sobre mi";

const identity = {
    firstName: "Keven CB",
    country: "Colombia",
    occupation: "Astro Developer",
    hobbies: ["Aprender", "Leer", "Fútbol"]
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Redacción de documentos"];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase"
---

<BaseLayout pageTitle={pageTitle}>
    <h2>Mi primer sitio web con Astro</h2>
    <p>
        Este proyecto lo iré actualizando paso a paso con el tutorial introductorio creado por el equipo de Astro.
        <a href="https://docs.astro.build/es/tutorial/2-pages/1/" target="_blank"></a>
    </p>

    <h3>Conoce algo acerca de mi:</h3>
    <ul>
        <li>Me llamo {identity.firstName}</li>
        <li>Vivo en {identity.country}</li>
        {
            identity.hobbies.length >= 2 &&
            <li>Dos de mis pasatiempos favoritos son: {identity.hobbies[0]} y {identity.hobbies[1]}</li>
        }
    </ul>
    <h3>Mis habilidades son:</h3>
    <ul>
        {skills.map((skill) => 
            <li class="skill">{skill}</li>
        )}
    </ul>

    {happy && <p>¡Estoy feliz aprendiendo Astro!</p>}
    {finished && <p>¡He terminado el tutorial!</p>}
    {goal === 3 ? <p><strong>Nota: </strong>Mi objetivo es terminar el tutorial en 3 dias.</p> : <p>Mi objetivo no son 3 días.</p> }
</BaseLayout>

<style define:vars={{skillColor, fontWeight, textCase}}>
    .skill{
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
    }
  </style>